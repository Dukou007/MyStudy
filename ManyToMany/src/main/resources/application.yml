server:
  port: 8088
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/zxl?autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: mysql
  redis:
    host: localhost
    port: 6379
logging:
  level: debug